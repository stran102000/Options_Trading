import React from 'react';

export default function TickerForm() {
  return (
    <div
      style={{
        marginTop: '2rem',
        padding: '1rem',
        border: '2px dashed #999',
        borderRadius: '4px',
        textAlign: 'center',
        color: '#666',
      }}
    >
      [ Ticker & Expiration Form ]
    </div>
  );
}